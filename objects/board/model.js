/**
 * Tavla Tahtası Modeli
 * Agent 2 - OBJECT_ENGINE
 * Dalga 1'de oluşturulacak
 */

// TODO: Dalga 1 - A2-1 fazında implement edilecek
export function createBoard(scene, config) {
    // BoxGeometry ile tahta gövdesi
    // Koyu kahve + açık kahve yüzeyler
    // Orta bölme (bar alanı)
    console.log('Board model placeholder');
}

export default { createBoard };